# Secrets only. Non-secret config (model ids, paths, hyperparameters, GCP
# project/region) lives in config/*.yaml per CLAUDE.md repo conventions —
# don't add it here.

# Judge / translation provider — DESIGN.md §9.4 (GPT-4o, decided in P1)
OPENAI_API_KEY=

# HF Spaces demo + HF Hub adapter upload — P6
HF_TOKEN=

# GCP auth: assumed to be Application Default Credentials
# (`gcloud auth application-default login`), not a service-account key file.
# If a key file ends up being needed instead, add its path here — never
# commit the key itself.
# GOOGLE_APPLICATION_CREDENTIALS=
